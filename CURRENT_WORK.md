# Current work

**Status:** idle — nothing in flight.

Last finished: **F32 — settings in categories.** `/einstellungen` is an overview of five
categories — general, planning, sign-in, data, extensions — each its own page with only
its own cards and data. On a phone a category leads back to the overview; on a wide
screen the category list stays beside every category page.

Next up: a release — F32 is on `main` only.

Notes for whoever comes next:

- The README's tests badge is still a static number.
- Not yet tried against a live Authentik, only against `oidc-provider`.

See `docs/WORKFLOW.md` for how this file is used.
