# Current work

**Status:** idle — nothing in flight.

Last finished: **F16 – Mobile polish, animation pass, accessibility audit**. The app now
has a skip link, dialogs that scroll on a phone instead of pushing their own title off
screen, spoken readbacks for values that change while typing, and CSS-only motion for
quantities that draw and shares that re-proportion.

Next up per `docs/PLAN.md`: **F17 – Docker image, compose file, `.env.example`, health
check, README with setup instructions.** That is the last item in Milestone D; F04b
(OIDC) remains deferred by request.

See `docs/WORKFLOW.md` for how this file is used.
