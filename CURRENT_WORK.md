# Current work

**Status:** idle — nothing in flight.

Last finished: **F03 – Domain engine**.
Next up per `docs/PLAN.md`: **F04 – Auth** (OIDC flow against Authentik, session cookie,
e-mail allowlist, local password fallback, `proxy.ts`, login screen).

See `docs/WORKFLOW.md` for how this file is used.
