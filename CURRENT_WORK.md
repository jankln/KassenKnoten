# Current work

**Status:** idle — nothing in flight.

Last finished: **F28 – A landing page in both languages**. English at `/`, German at
`/de/`, one shared `style.css`, a switcher in the header of each and `hreflang` for search
engines. Each page shows screenshots of the interface in its own language —
`docs/media/` is German, `docs/media/en/` is the same screens in English.

Open on `docs/PLAN.md`: **F04b – OIDC against Authentik**, deferred by request.

Note for whoever comes next: the two pages share a structure on purpose, so a change to
one is a change to make in the other. The stylesheet is the only thing that is genuinely
shared; the copy is not, and it is not worth pretending otherwise for two languages.

See `docs/WORKFLOW.md` for how this file is used.
